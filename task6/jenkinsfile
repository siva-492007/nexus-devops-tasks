pipeline {
    agent any
    stages{
        // stage('Initialize packer'){
        //     steps{
        //         sh "packer init ."
        //     }
        // }

        stage('Build Wordpress AMI'){
            steps{
                sh "packer build aws-ubuntu.pkr.hcl"
            }
        }
    }
}