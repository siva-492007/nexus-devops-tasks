pipeline {
    agent any
    stages{
        // stage('cd to task6'){
        //     steps{
        //         sh "cd task6"
        //     }
        // }

        stage('Build Wordpress AMI'){
            steps{
                sh "packer build packer/aws-ubuntu.pkr.hcl"
            }
        }
    }
}