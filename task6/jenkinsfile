pipeline {
    agent any
    stages{
        stage('Initialize packer'){
            steps{
                sh "packer init /task6/packer"
            }
        }

        stage('Build Wordpress AMI'){
            steps{
                sh "packer build /task6/packer/aws-ubuntu.pkr.hcl"
            }
        }
    }
}