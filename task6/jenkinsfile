pipeline {
    agent any
    stages{
        stage('Initialize packer'){
            steps{
                sh "packer init /task6"
            }
        }

        stage('Build Wordpress AMI'){
            steps{
                sh "packer build /task6packer/aws-ubuntu.pkr.hcl"
            }
        }
    }
}